namespace TimeMaker.Models
{
    public enum UploadStatus
    {
        Pending,
        Completed,
        Failed,
        Ignored
    }

    public enum TimyMode
    {
        Stopwatch,
        Backup
    }
}

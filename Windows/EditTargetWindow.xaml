<Window x:Class="TimeMaker.Windows.EditTargetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Zmena cieľových bodov" Height="240" Width="350" ResizeMode="NoResize" WindowStartupLocation="CenterOwner" Topmost="True" Icon="../Icons/favicon.ico">
    <GroupBox Header="Zmena cieľových bodov">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <GroupBox Grid.Row="0" Header="Cieľové meracie body">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" x:Name="FirstTargetLabel" Content="C0 / C0M bod:" Margin="5"/>
                    <ComboBox Grid.Row="0" x:Name="StartPoint" Margin="138,6,5,5" Width="120"/>
                    <Label Grid.Row="1" x:Name="SecondTargetLabel" Content="C1 / C1M bod:" Margin="5"/>
                    <ComboBox Grid.Row="1" x:Name="FinishPoint" Margin="138,6,5,5" Width="120"/>
                    <Label Grid.Row="2" x:Name="ThirdTargetLabel" Content="RT / RTM bod:" Margin="5"/>
                    <ComboBox Grid.Row="2" x:Name="RunTimePoint" Margin="138,6,5,5" Width="120" IsEnabled="False"/>
                </Grid>
            </GroupBox>
            <Grid Grid.Row="1">
                <Button Content="Uložiť" Click="Save" HorizontalAlignment="Right" Margin="0,10,10,0" Padding="10 5" Width="93"/>
            </Grid>
        </Grid>
    </GroupBox>
</Window>

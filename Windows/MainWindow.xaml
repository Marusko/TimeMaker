<Window x:Class="TimeMaker.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:TimeMaker.ViewModels"
        mc:Ignorable="d"
        Closing="MainWindow_OnClosing"
        Title="Time Maker" Height="480" Width="860"
        MinHeight="380" MinWidth="740"
        FontFamily="Segoe UI" FontSize="13"
        Foreground="{StaticResource TextPrimaryBrush}"
        TextOptions.TextFormattingMode="Ideal"
        UseLayoutRounding="True"
        Background="{StaticResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen" Icon="../Icons/favicon.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top toolbar -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderSoftBrush}" BorderThickness="0,0,0,1"
                Padding="16,10">
            <DockPanel>
                <CheckBox DockPanel.Dock="Right" Content="Upozorniť na nenahraté"
                          Checked="SetNotify" Unchecked="SetNotNotify" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="BtnAddFile" Content="Pridať CSV" Click="BtnAddFile_Click"
                            Padding="14,7" IsEnabled="False"/>
                    <Button x:Name="BtnAddTimy" Content="Pridať Timy" Click="BtnAddTimy_Click"
                            Padding="14,7" Margin="8,0,0,0" IsEnabled="False"/>
                    <Border Width="1" Background="{StaticResource BorderSoftBrush}" Margin="14,2"/>
                    <Button x:Name="BtnRaceSettings" Content="Nastavenia RR" Click="BtnRaceSettings_Click"
                            Style="{StaticResource SecondaryButtonStyle}" Padding="14,7"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Aktívne zdroje" FontSize="17" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,10,0,0" Padding="6">
                <ListView x:Name="ListViewSources">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="4,7"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Názov" DisplayMemberBinding="{Binding Name}" Width="100"/>
                            <GridViewColumn Header="Typ" DisplayMemberBinding="{Binding Type}" Width="80"/>
                            <GridViewColumn Header="Zdroj" DisplayMemberBinding="{Binding Source}" Width="100"/>
                            <GridViewColumn Header="Cieľový bod" DisplayMemberBinding="{Binding Target}" Width="100"/>
                            <GridViewColumn Header="Stav" DisplayMemberBinding="{Binding Status}" Width="100"/>
                            <GridViewColumn Header="Akcie" Width="300">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type viewModels:SourceItemViewModel}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock VerticalAlignment="Center" Margin="0,0,12,0">
                                                <Hyperlink Click="StartSource" IsEnabled="{Binding IsNotRunning}">
                                                    <Run Text="Spustiť"/>
                                                </Hyperlink>
                                            </TextBlock>
                                            <TextBlock VerticalAlignment="Center" Margin="0,0,12,0">
                                                <Hyperlink Click="StopSource" IsEnabled="{Binding IsRunning}">
                                                    <Run Text="Zastaviť"/>
                                                </Hyperlink>
                                            </TextBlock>
                                            <TextBlock VerticalAlignment="Center" Margin="0,0,12,0">
                                                <Hyperlink Click="RemoveSource" IsEnabled="{Binding IsNotRunning}">
                                                    <Run Text="Odstrániť"/>
                                                </Hyperlink>
                                            </TextBlock>
                                            <TextBlock VerticalAlignment="Center" Margin="0,0,12,0">
                                                <Hyperlink Click="EditTarget">
                                                    <Run Text="Upraviť"/>
                                                </Hyperlink>
                                            </TextBlock>
                                            <TextBlock VerticalAlignment="Center">
                                                <Hyperlink Click="ViewRawSource">
                                                    <Run Text="Zobraziť"/>
                                                </Hyperlink>
                                            </TextBlock>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderSoftBrush}" BorderThickness="0,1,0,0"
                Padding="16,7">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Matúš Suský" Foreground="{StaticResource TextSecondaryBrush}"/>
                <Border Width="1" Background="{StaticResource BorderSoftBrush}" Margin="12,1"/>
                <TextBlock x:Name="VersionText" Foreground="{StaticResource TextSecondaryBrush}"/>
                <Border Width="1" Background="{StaticResource BorderSoftBrush}" Margin="12,1"/>
                <TextBlock>
                    <Hyperlink Click="OpenJson">
                        <Run Text="Newtonsoft.Json"/>
                    </Hyperlink>
                </TextBlock>
                <Border Width="1" Background="{StaticResource BorderSoftBrush}" Margin="12,1"/>
                <TextBlock>
                    <Hyperlink Click="OpenIo">
                        <Run Text="System.IO.Ports"/>
                    </Hyperlink>
                </TextBlock>
                <Border Width="1" Background="{StaticResource BorderSoftBrush}" Margin="12,1"/>
                <TextBlock>
                    <Hyperlink Click="OpenNtf">
                        <Run Text="Microsoft.Toolkit.Uwp.Notifications"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>
